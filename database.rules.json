{
  "rules": {
    "presence": {
      "$pairId": {
        "$userId": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid === $userId"
        }
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
